[project]
name = "msys-settings"
version = "0.5.3"
description = "Dependency-free MSYS settings application for the language-neutral mIPC contract"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["files/app", "../msys-sdk"]
